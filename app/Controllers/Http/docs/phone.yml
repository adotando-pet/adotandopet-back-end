/phone:
  get:
    tags:
      - Phone
    description: List all registered phones
    produces:
      - application/json
    responses:
      200:
        description: List of phones
  post:
    tags:
      - Phone
    description: Create new Phone
    produces:
      - application/json
    parameters:
      - name: userId
        description: Id owner of number
        in: formData
        required: true
        type: integer
    responses:
      200:
        description: number create
