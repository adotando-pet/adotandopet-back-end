/session:
  post:
    tags:
      - Session
    description: Create new session
    produces:
      - application/json
    parameters:
      - name: email
        description: email to login.
        in: formData
        required: true
        type: string
      - name: password
        description: User's password.
        in: formData
        required: true
        type: string
    responses:
      200:
        description: login
